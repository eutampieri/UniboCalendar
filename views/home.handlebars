<div class="container" id="main-container">
  <div class="header">
    <h1>Unibo Calendar</h1>
    <h5>Vuoi inserire i tuoi appuntamenti universitari nel tuo calendario (iCloud, Google, ecc)?<br>Riempi i campi qui
      sotto per cominciare</h5>
  </div>
  <div class="container" id="body">
    <form id="form_calendar" action="/course" method="post" onchange="checkFormValidity();">
      <div class="row dropdown">
        <button type="button" class="btn btn-light dropdown-toggle" data-toggle="dropdown">
          --- Seleziona Scuola ---
        </button>
        <div id="areas" class="dropdown-menu" data-toggle="dropdown" name="areas" onchange="getCoursesGivenArea();">
          {{#each areas}}
          <a class="dropdown-item" onclick="getCoursesGivenArea();" value="{{this}}">{{this}}</a>
          {{/each}}
        </div>
      </div>
      <div class="row">
        <select id="courses" class="form-control" name="courses" onchange="getYearsAndCurriculaGivenCourse();">
        </select>
        <img id="courses-loading" src="../../loader.gif" alt="Stiamo scaricando i corsi..." />
      </div>
      <div class="row">
        <select id="years" class="form-control" name="years">
        </select>
      </div>
      <div class="row" style="position: relative;">
        <select id="curricula" class="form-control" name="curricula">
        </select>
        <img id="curricula-loading" src="../../loader.gif" alt="Stiamo scaricando i curricula..." />
      </div>
      <div class="row">
        <input type="submit" id="submit-form-button" class="btn btn-primary" value="Avanti" disabled>
      </div>
    </form>
  </div>
  <div id="low-connection">Connessione debole...</div>
</div>